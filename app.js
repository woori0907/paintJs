const canvas = document.getElementById("jsCanvas");

function onMouseMove(event){
    // clientX, Y : 윈도우 전체 안에서의 좌표
    // canvas 안에서와 윈도우에서의 좌표값이 다르기 때문에 
}

if(canvas){
    canvas.addEventListener("mousemove", onMouseMove);

}